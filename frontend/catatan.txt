 npm install primevue @primevue/themes

 // app.use(PrimeVue)
app.use(PrimeVue, {
    theme: {
        preset: Aura
    }
});

npm install primeicons
npm install primeflex

import primeflex/primeflex.css



…or create a new repository on the command line
echo "# testAja" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/minionxapp/testAja.git
git push -u origin main

…or push an existing repository from the command line
git remote add origin https://github.com/minionxapp/mevn.git
git branch -M main
git push -u origin main

bila npm run dev already use
sudo lsof -i :3000
kill -9 <PID>